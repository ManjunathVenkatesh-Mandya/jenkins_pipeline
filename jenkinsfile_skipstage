boolean skipStage = true

pipeline{
    agent any

    stages{
        stage('stage1'){
            steps{
                script{
                    try{
                        echo "my name is Manjunath"
                        sh 'exit 1'
                    }
                    catch (err) {
                        echo "caught error: ${err}"
                         skipStage = false
                    }
                }
            }
        }

        stage('stage2'){
            steps{
                echo "i am INDIAN"
            }
        }
    }
}