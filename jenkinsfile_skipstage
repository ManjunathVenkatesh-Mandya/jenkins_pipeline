boolean skipStage = true

pipeline{
    agent any

    stages{
        stage('stage1'){
            steps{
                script{
                    try{
                        
                        sh 'ls -lrt'
                    }
                    catch (err) {
                        echo "caught error: ${err}"
                         skipStage = false
                    }
                }
            }
        }

        stage('stage2'){
            steps{
                echo "i am INDIAN"
            }
        }
    }
}